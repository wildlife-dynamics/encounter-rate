# AUTOGENERATED BY ECOSCOPE-WORKFLOWS; see fingerprint in README.md for details

[build-system]
requires = ["hatchling>=1.18.0,<2", "ruamel.yaml>=0.17.0,<1"]
build-backend = "hatchling.build"

[project]
name = "ecoscope-workflows-encounter-rate-workflow"
dynamic = ["version"]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ecoscope-workflows-encounter-rate-workflow = "ecoscope_workflows_encounter_rate_workflow.cli:cli"

[tool.hatch.metadata.hooks.custom]
path = "hatch_build.py"

[tool.hatch.build.targets.wheel]
packages = ["ecoscope_workflows_encounter_rate_workflow"]
